# 延时摄影工作流程图

## 整体工作流程

```
用户配置阶段
    ↓
┌─────────────────────────────────────┐
│ 1. 用户在配置界面设置                │
│    - 选择摄像头实体                  │
│    - 选择触发模式                    │
│    - 选择触发实体 (状态变化模式)      │
│    - 设置其他参数                    │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 2. 开关实体创建                      │
│    - TimelapseSwitch 初始化          │
│    - 显示为 OFF 状态                 │
│    - 显示配置的触发模式信息           │
└─────────────────────────────────────┘
    ↓
用户操作阶段
    ↓
┌─────────────────────────────────────┐
│ 3. 用户开启开关                      │
│    - 点击开关 UI                     │
│    - 或调用 switch.turn_on 服务      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. 开关调用协调器                    │
│    - async_turn_on() 方法执行        │
│    - 传递配置的触发模式和触发实体     │
│    - 调用 coordinator.start_timelapse│
└─────────────────────────────────────┘
    ↓
延时摄影执行阶段
    ↓
┌─────────────────────────────────────┐
│ 5. 协调器启动延时摄影任务             │
│    - 创建任务ID                      │
│    - 设置输出目录                    │
│    - 根据触发模式选择执行路径         │
└─────────────────────────────────────┘
    ↓
    ┌─────────────────┐    ┌─────────────────┐
    │  间隔模式        │    │  状态变化模式    │
    │  (interval)     │    │  (state_change) │
    └─────────────────┘    └─────────────────┘
           ↓                        ↓
    ┌─────────────────┐    ┌─────────────────┐
    │ 按固定时间间隔   │    │ 设置状态监听器   │
    │ 捕获图片        │    │ 监听触发实体     │
    └─────────────────┘    └─────────────────┘
           ↓                        ↓
    ┌─────────────────┐    ┌─────────────────┐
    │ 每N秒拍照一次    │    │ 状态变化时拍照   │
    └─────────────────┘    └─────────────────┘
           ↓                        ↓
    ┌─────────────────────────────────────────┐
    │ 6. 图片保存和进度更新                    │
    │    - 保存图片到临时目录                  │
    │    - 更新帧计数                         │
    │    - 更新开关属性                       │
    └─────────────────────────────────────────┘
    ↓
用户停止阶段
    ↓
┌─────────────────────────────────────┐
│ 7. 用户关闭开关                      │
│    - 点击开关 UI                     │
│    - 或调用 switch.turn_off 服务     │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 8. 清理和视频生成                    │
│    - 清理状态监听器                  │
│    - 使用 FFmpeg 生成视频            │
│    - 上传到 Google Photos (可选)     │
│    - 清理临时文件                    │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 9. 完成                             │
│    - 开关显示为 OFF 状态             │
│    - 显示完成信息和视频路径           │
└─────────────────────────────────────┘
```

## 关键要点

### 🔑 开关控制
- **延时摄影任务的启动和停止始终通过开关控制**
- 开关状态反映延时摄影的运行状态
- 用户体验与原有功能完全一致

### ⚙️ 触发模式配置
- 触发模式在配置阶段设置，不是运行时选择
- 开关使用配置中预设的触发模式
- 支持通过配置界面修改触发模式

### 📸 图片捕获差异
- **间隔模式**: 定时器触发，每N秒拍照
- **状态变化模式**: 事件触发，状态变化时拍照

### 🔄 向后兼容
- 现有配置自动使用间隔模式
- 所有原有功能保持不变
- 新功能作为可选增强

## 用户交互示例

### 配置阶段
```
用户: 进入集成配置界面
系统: 显示配置选项
用户: 选择 "实体状态变化" 模式
用户: 选择 "binary_sensor.motion_detector" 作为触发实体
用户: 保存配置
```

### 使用阶段
```
用户: 开启延时摄影开关
系统: 开关变为 ON 状态
系统: 开始监听 motion_detector 状态变化
系统: 检测到运动 → 自动拍照
系统: 更新帧计数显示
用户: 关闭延时摄影开关
系统: 停止监听，生成视频，开关变为 OFF
```

这个工作流程确保了用户体验的一致性，同时提供了更灵活的图片捕获方式。