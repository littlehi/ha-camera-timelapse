{
  "config": {
    "step": {
      "user": {
        "title": "Camera Timelapse Setup",
        "description": "Select a camera entity and configure default settings for timelapse recording.",
        "data": {
          "camera_entity_id": "Camera Entity",
          "default_interval": "Default Interval (seconds)",
          "default_duration": "Default Duration (minutes)",
          "default_output_path": "Default Output Path",
          "debug_mode": "Enable Debug Mode",
          "upload_to_google_photos": "Upload to Google Photos",
          "google_photos_album": "Google Photos Album",
          "google_photos_config_entry_id": "Google Photos Account",
          "delete_after_upload": "Delete local file after successful upload"
        }
      }
    },
    "error": {
      "not_a_camera": "Selected entity is not a camera",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "This camera is already configured for timelapse",
      "no_cameras": "No camera entities found in your system"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Camera Timelapse Options",
        "description": "Adjust default settings for timelapse recording.",
        "data": {
          "default_interval": "Default Interval (seconds)",
          "default_duration": "Default Duration (minutes)",
          "default_output_path": "Default Output Path",
          "debug_mode": "Enable Debug Mode",
          "upload_to_google_photos": "Upload to Google Photos",
          "google_photos_album": "Google Photos Album",
          "google_photos_config_entry_id": "Google Photos Account",
          "delete_after_upload": "Delete local file after successful upload"
        }
      }
    }
  }
}